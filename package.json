{
  "name": "yape-notification-monitor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix frontend",
    "build": "npm run build --prefix frontend"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1"
  }
}
